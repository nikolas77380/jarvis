# Incidents

Create observations with `scripts/memory-record.sh`. An incident stays `open` until explicitly
promoted or dismissed. Open incidents are excluded from normal context unless
`memory-context.sh --include-incidents` is requested.
