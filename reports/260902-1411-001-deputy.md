# 260902-1411-001 — deputy report

## What changed

Adapted the five Yavo project-local JS/TS roles (`projects/yavo/agents/{nestjs-engineer,
nestjs-reviewer, nextjs-engineer, nextjs-reviewer, deps-researcher}.md`, read from the main harness
checkout at `/Users/nikolaykipniak/projects/harness/projects/yavo/agents/`, since this worktree does
not carry `projects/yavo`) into reusable GLOBAL Jarvis roles:

- `agents/nestjs-engineer.md` — replaced the previously-committed global role, which was in fact a
  verbatim copy of Yavo's local role (`yavo-api`, `yavo-analyze-worker`, `master` hardcoded). Rewrote
  it to house style: `{{APP_PATH}}`, `{{BASE_BRANCH}}` placeholders, project-derived validation
  commands, no hardcoded repo/DB names. Kept TDD discipline, module/layer boundaries, DTO validation,
  worker idempotency, migration-safety hard constraints, and the rule-of-three extraction guidance.
- `agents/nestjs-reviewer.md` — same treatment, logic-tier review discipline preserved (bounded
  reading, delta-only fix rounds, round ceiling of two, the producer/consumer trust defect class).
- `agents/nextjs-engineer.md` (new) — adapted from Yavo's App Router role. Dropped the
  `yavo-admin`/`yavo-landing` two-project split and the "shared production DB via client header"
  specifics; generalized to "your brief and this project's CLAUDE.md say the actual topology" with
  the same hard-constraint shape (no prod writes, no migrations here unless brief says so, no
  middleware changes, etc.), and a branch stated for when a project's checkout has no git repo.
- `agents/nextjs-reviewer.md` (new) — logic-tier counterpart, same generalization approach.
- `agents/deps-researcher.md` (new) — adapted from Yavo's researcher. Dropped the Yavo-specific
  `.claude/stack.yml` requirement and the `Skill` tool calls into Yavo-local criteria skills
  (`react-deps-criteria` etc., which don't exist globally); replaced with inline universal criteria
  (peer deps, maintenance signal, types, license, transitive weight, exit cost, bundle-size /
  server-runtime fit) and a project-local-policy-file lookup that degrates gracefully when absent.
  Tools restricted to Read/Grep/Glob/Bash/WebSearch/WebFetch — no Edit/Write — and the role states
  explicitly it never installs or edits.

## Routing changes

- `RULES.md` — added `deps-researcher` to the agent-pipeline bullet list: required before
  adopting/replacing an npm package and before a major upgrade; patch/minor route to it only under
  compatibility or security uncertainty; read-only, ranked evidence-backed recommendation + install
  command, never installs/edits.
- `agents/orchestrator.md` — added the same routing rule under "2. Delegate".

## Tests

Extended `tests/role-resolution.test.sh` (appended, did not touch the existing alpha/beta fixture
test) with a second suite that runs against the real `agents/` tree:

- Frontmatter validity (opens with `---`, has a closing `---`, has `name:`) for all five roles.
- `model:`/`codex_model:` tiers match the preserved Yavo tiers (engineers+researcher: sonnet;
  reviewers: opus; all: gpt-5.6-sol).
- No Yavo-specific strings (`yavo`, `yavo-api`, `yavo-admin`, `yavo-landing`,
  `yavo-analyze-worker`, `nikolas77380`) in any of the five files.
- No raw `gh <verb>` usage (only `gh-axi`).
- `deps-researcher`'s `tools:` line excludes `Edit`/`Write`, and the file states "read-only" and
  "never install".
- `agent-spawn.sh` still resolves a project-local `agents/` override before falling back to the
  harness-shared one (sanity check that the global-fallback mechanism these roles rely on is
  unchanged).
- RULES.md and orchestrator.md both mention `deps-researcher` and "major" (upgrade routing).

## Test results

```
$ bash tests/role-resolution.test.sh
role resolution tests: ok
global JS/TS role fixture tests: ok

$ bash scripts/plan-check.sh
plan-check: ok — every plan/*.md is tracked and indexed

$ bash scripts/owns-check.sh
owns-check: ok — no overlapping claims among 2 active card(s), 6 claim(s)
```

## PR

https://github.com/nikolas77380/jarvis/pull/4 (branch `harness/260902-1411-001` → `main`)

## Judgment calls

- The existing `agents/nestjs-engineer.md`/`nestjs-reviewer.md` in this repo were NOT genuinely
  global — they were byte-for-byte Yavo-local content (hardcoded `yavo-api`, `master`, currency-bug
  anecdotes). Treated the task's "existing global NestJS roles" instruction as meaning these were the
  starting point to fix, not a baseline to preserve unchanged.
- `deps-researcher`'s Yavo version depends on Yavo-local Skill files for stack-specific criteria.
  Since the shared harness must not depend on Yavo, replaced that mechanism with inline universal
  criteria plus stack-conditional bundle-size/server-runtime checks, rather than inventing new global
  skill files not requested by the task card.
- Next.js roles generalize the "yavo-admin has no git repo" case into a brief-driven branch rather
  than assuming every project has one, since that was a real (not hypothetical) shape in the source
  material worth preserving generically.

## Reconciliation with `main` — 2026-09-03

`origin/main` had advanced to `b7f3706` (merged PR #5: MCP consent inheritance + capability
preflight). Reconciled by **merging** `origin/main` into `harness/260902-1411-001` (`5a71d25`) rather
than rebasing: the branch is already pushed under open PR #4, and force-pushes are not
pre-authorized.

### What actually conflicted

Only `plan/INDEX.md`, and only over table-column widths — `main` widened them to fit the new
`260902-1545-001` row. Resolved to `main`'s version, a strict superset; the `260902-1411-001` row is
byte-identical in both.

`RULES.md` and `agents/orchestrator.md` auto-merged with no conflict because the two sides touched
different regions. Verified by grep that neither concern was dropped:

| concern | RULES.md | agents/orchestrator.md |
| --- | --- | --- |
| this branch: `deps-researcher` routing | lines 33-37 | lines 131-134 |
| `main` (PR #5): anti-proxy / capability preflight | lines 51-58 | lines 99-102 |

Everything else the branch had added — the `OVERVIEW.md` entry, this card, the two
`260902-1204-001` artifacts and this report — is already byte-identical on `main` (it reached `main`
via PR #5's branch), so those files dropped out of the PR diff on their own.

### PR #4 diff after the merge

Exactly the eight files this card claims under `Owns:` — 929 insertions, 167 deletions:
`RULES.md`, `agents/orchestrator.md`, the five role files, `tests/role-resolution.test.sh`.

### Checks

```
$ for t in tests/*.test.sh; do bash "$t"; done
24/24 PASS
```

That includes `main`'s four new suites run against the merged rule files —
`tests/anti-proxy-rule.test.sh`, `tests/capability-preflight.test.sh`,
`tests/capability-aware-dispatch.test.sh`, `tests/capability-preflight-handoff.test.sh` — plus
`tests/mcp-inheritance.test.sh`.

```
$ bash scripts/plan-check.sh
plan-check: ok — every plan/*.md is tracked and indexed

$ bash scripts/owns-check.sh
owns-check: ok — no overlapping claims among 3 active card(s), 6 claim(s)
```

### Judgment calls and things left alone

- **No `capabilities:` frontmatter added to the five new roles.** `role_capabilities()` in
  `scripts/agent-engine-lib.sh` treats an absent field as "no capabilities required, no preflight" —
  the mechanism is for gated live MCP access (`capabilities: figma` on `design-qa`). `deps-researcher`
  uses the built-in `WebSearch`/`WebFetch` tools, which the preflight prompt has no probe vocabulary
  for, so declaring a capability there would fail closed for no benefit.
- **No `OVERVIEW.md` entry.** A branch reconciliation is not an event; the merge of PR #4 is, and
  `OVERVIEW.md` is not in this card's `Owns:` set. Left to the lead at merge time.
- **`plan/INDEX.md` on `main` still lists `260902-1545-001` as `open`** although PR #5 is merged.
  That is a pre-existing staleness on `main`, unrelated to this card, so it was carried through
  untouched rather than silently corrected inside this PR. Flagging it for the lead.
