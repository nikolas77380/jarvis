# 260902-1411-001 — shell-engineer report, round-1 fix

Fixed all findings from `reports/260902-1411-001-reviewer.md` (round 1, verdict REQUEST_CHANGES).

## Blockers closed

1. **Inert `tools:` frontmatter.** Chose the "keep declarative, be honest" direction (the reviewer
   offered either this or wiring real `--allowedTools` enforcement into `engine_start()` as
   acceptable) — enforcing tool restriction in `scripts/agent-engine-lib.sh` is shared code every
   role in the harness depends on and is outside this card's `Owns:` list, so implementing it would
   have been exactly the "blast radius beyond your diff" the engineer roles themselves are told to
   stop and ask about. Instead: `RULES.md`, `deps-researcher.md`, `nestjs-reviewer.md`, and
   `nextjs-reviewer.md` now all say plainly that `tools:`/"read-only" is a role instruction, not a
   runtime restriction — every session runs under `bypassPermissions` with `Bash` available, so
   nothing technical stops a write. The test comment in `role-resolution.test.sh` was reworded to
   say it checks the declaration, never enforcement.
2. **`nestjs-reviewer.md` told a read-only reviewer to run migrations.** Deleted the copy-pasted
   sentence at what was lines 41-42; the reviewer already has the right instrument (read the
   migration, reason about rolling-deploy safety) a few sections down.

## Should-fix closed

3. Both new reviewers now emit `Verdict: APPROVE | REQUEST_CHANGES`, matching every other reviewer
   role and the literal gate token `RULES.md`/`orchestrator.md` check for.
4. Both now instruct writing `reports/<task-id>-<role>.md` plus a ≤15-line return, matching
   `reviewer.md`.
5. Both engineer roles' npm-adoption step now says "stop and report that the task needs a
   `deps-researcher` recommendation" instead of an instruction the engineer cannot itself act on.
6. The Next.js constraint (both engineer and reviewer) now names `middleware.ts` **or** `proxy.ts`
   on Next 16+, keyed on behavior rather than a filename current Next.js no longer uses.
7. `nextjs-engineer.md`'s verify step now leads with the project's lint script or `eslint`/`biome`
   directly, noting `next lint` was removed in Next 16 and `next build` doesn't cover for it.
8. Deleted the `role-resolution.test.sh` assertion that grepped `agent-spawn.sh` for a pattern that
   only matched a `die` message — zero marginal coverage, and it broke on a wording-only edit
   (verified: M4/M5 mutation results in the round-1 report). The pre-existing alpha/beta fixture
   test already exercises real `role_path()` resolution.

## Consider items addressed

9. Softened the `.claude/stack.yml` wording in `deps-researcher.md` to lead with "wherever this
   project's `CLAUDE.md` points."
10. Added one line to `RULES.md` saying the lead pastes the researcher's verdict block into the
    task card (or `docs/decisions/`) before dispatching the install, so "recommendation on record"
    resolves to an openable path.

Did not touch the deputy's round-0 report (`reports/260902-1411-001-deputy.md`) — it's a historical
account of what was done at the time, not living documentation to rewrite.

## Checks run

- `bash tests/role-resolution.test.sh` — ok, both suites.
- `for t in tests/*.test.sh; do bash "$t"; done` — 24/24 pass.
- `bash scripts/plan-check.sh` — ok.
- `bash scripts/owns-check.sh` — ok, no overlapping claims.
- `bash scripts/review-rounds.sh 260902-1411-001` — ledger reads 1 recorded / 0 ran (round 1 ran in
  a different Herdr tab, so its transcript isn't in this session's local project directory); one
  round left before the ceiling either way.

## Overview entry

## Global JS/TS agent roles — round-1 fixes — 2026-09-04

What: Closed both blockers and all six should-fix findings from round-1 review of PR #4
(unenforced-but-certified read-only authority, a reviewer told to run migrations it forbids itself
from running, wrong verdict token, missing report contract, an impossible engineer dispatch
instruction, a pre-Next-16 filename constraint, a removed lint command, and a vacuous test
assertion), plus two "consider" items.
Why: Tool-restriction enforcement was rejected as a fix in favor of stating the true (unenforced,
instruction-based) authority model — implementing it touches shared runtime code outside this
card's `Owns:` and outside this fix round's scope.
Deploy impact: none.
Files: RULES.md, agents/deps-researcher.md, agents/nestjs-reviewer.md, agents/nextjs-reviewer.md,
agents/nestjs-engineer.md, agents/nextjs-engineer.md, tests/role-resolution.test.sh.
