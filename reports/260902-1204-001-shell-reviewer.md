# 260902-1204-001 - shell-reviewer, round 1 of 2

**Verdict:** REQUEST_CHANGES
**Diff reviewed:** `main...c29e209` (PR #3), implementation tip `03dfb01`
**Scope:** `bootstrap.sh`, `bin/jarvis`, `tests/bootstrap*.test.sh`, `README.md`,
`docs/herdr-runtime.md`. Report and plan files out of review scope.

## Checks re-run by the reviewer (not taken from the engineer summary)

- `bash tests/bootstrap.test.sh` - 9/9 cases ok.
- `bash tests/bootstrap-interactive.test.sh` - 2/2 cases ok.
- Full suite, every `tests/*.test.sh` individually - 21/21 PASS.
- `shellcheck bootstrap.sh` - clean, no output.
- `shellcheck bin/jarvis` - only pre-existing findings outside the diff (SC1091 x3 on the
  `. "$ROOT/scripts/..."` lines, SC2034 on `HARNESS_HERDR_SESSION` at line 84). The diffed hunk
  (lines 3-12) is clean.
- `shellcheck tests/bootstrap*.test.sh` - four SC2016 info notes on the deliberately
  single-quoted `export PATH="$HOME/..."` literals. Correct as written.
- `scripts/plan-check.sh` - ok. `scripts/owns-check.sh` - ok, 1 active card, 4 claims,
  no overlap. The card declares `bin/jarvis` under `Owns:` with the user approval recorded,
  so the shared-entry-point edit is properly claimed.

The engineer's headline figures hold up: 21/21 and a clean `shellcheck bootstrap.sh` are what I
observed too.

## Findings

### 1. BLOCKING - the fresh-macOS path aborts after a successful installer run

`bootstrap.sh:60-78` (`ensure_herdr`, `ensure_claude`) confirm the install with `command -v` against
the *current process* PATH. `ensure_path_export` (`bootstrap.sh:80-88`, called at line 155) only
appends to `~/.zprofile`; it never adds `$HOME/.local/bin` to the running shell's PATH, and it runs
*after* both installers anyway.

Both official installers put their binary in `~/.local/bin`. Measured on this host:

```
$ command -v claude   -> /Users/nikolaykipniak/.local/bin/claude
$ command -v herdr    -> /Users/nikolaykipniak/.local/bin/herdr
```

That is the very directory `verify` (`bootstrap.sh:137-142`) already assumes may be absent from PATH.
So on a genuinely fresh macOS, where `~/.local/bin` is not on PATH yet, the installer succeeds and
bootstrap then declares it failed.

Reproduced with a fixture whose fake installers write into `$HOME/.local/bin` (as the real ones do)
while PATH excludes it:

```
installing herdr: curl -fsSL https://herdr.dev/install.sh | sh
error: herdr installation did not put herdr on PATH
--- what the installer actually produced ---
-rwxr-xr-x  1 ... $HOME/.local/bin/herdr
```

`ensure_claude` fails identically once `herdr` is out of the way. This is the primary scenario the
script exists for, and it fails closed with a misleading message.

Why the suite misses it: `fake_curl` writes its executables into `$BOOTSTRAP_TEST_BIN`
(`tests/bootstrap.test.sh:88` and `:95`), and `run_bootstrap` sets `PATH="$dir/bin"` to that same
directory (`tests/bootstrap.test.sh:113`). The fixture installer therefore always installs onto PATH,
which no real installer guarantees. The test proves the fake, not the behaviour.

Fix: add `export PATH="$HOME/.local/bin:$PATH"` (idempotently) inside `bootstrap.sh` before the
installer steps, so the process can see what the installers wrote. Add a case whose fake installer
targets `$HOME/.local/bin` while PATH excludes it.

### 2. BLOCKING - `mv` onto a directory destination moves the link inside it instead of replacing it

`bootstrap.sh:119-121`:

```sh
tmp_link="$local_bin/.jarvis.bootstrap.$$"
ln -s "$JARVIS_BIN" "$tmp_link"
mv "$tmp_link" "$JARVIS_LINK"
```

BSD `mv` resolves a directory destination (and follows a symlink to one) and moves the source
*into* it. Measured on this macOS:

```
$ ln -s /etc/hosts tmp_link && mv tmp_link dest_dir
$ ls dest_dir      -> tmp_link -> /etc/hosts          # dest_dir intact, link moved inside

$ ln -s "$PWD/realdir" jarvis && ln -s /etc/hosts tmp2 && mv tmp2 jarvis
$ ls -la .         -> jarvis -> .../realdir           # unchanged
$ ls -la realdir   -> tmp2 -> /etc/hosts              # link landed inside the target
```

Two reachable paths:

- `bootstrap.sh:116-118` - the destination is a **directory**. `confirm_replace` asks, the user
  says yes, and nothing is replaced: `~/.local/bin/jarvis` stays a directory with a stray
  `.jarvis.bootstrap.<pid>` symlink inside it. `verify` then dies at `bootstrap.sh:133`
  ("is not a symlink") after the mutation, so a granted confirmation produces both a failure and
  litter.
- `bootstrap.sh:109-115` - a stale symlink pointing at a **directory**. This branch does not prompt
  at all ("a stale symlink is always safe to repair"), so the repair silently does nothing and drops
  a stray symlink inside whatever directory the old link pointed to.

Neither case is covered: test 8 uses a symlink to a non-existent file
(`tests/bootstrap.test.sh:263`), and test 9 plus both interactive cases use a regular file.

Fix: unlink the destination explicitly before the atomic rename (BSD `mv` has no `-T`), or use
`ln -sfn`, and extend `ensure_symlink`'s handling to reject a directory destination outright rather
than routing it through `confirm_replace` as if replacement would work. Add both directory cases to
the suite.

### 3. NON-BLOCKING - `ZDOTDIR` is ignored, so the PATH export can land in an unread file

`bootstrap.sh:81` uses `$HOME/.zprofile` unconditionally. When `ZDOTDIR` is set, zsh reads
`$ZDOTDIR/.zprofile` and never looks at `$HOME/.zprofile`, so the export is written to a file that
is never sourced and `jarvis` silently never reaches PATH, while bootstrap reports success and
`verify` prints the reassuring "added to ~/.zprofile" line.

The repo already has the convention: `bin/jarvis install-alias` uses `${ZDOTDIR:-$HOME}/.zshrc`
(`bin/jarvis:108`). Use `${ZDOTDIR:-$HOME}/.zprofile` for consistency, and reflect the resolved path
in the messages at `bootstrap.sh:83`, `:87`, and `:141`.

### 4. MINOR - `~/.zprofile` is mutated before the symlink step can refuse

`main` runs `ensure_path_export` (line 155) before `ensure_symlink` (line 156). When the destination
is unexpected and confirmation is refused (`bootstrap.sh:117`), the run fails with `~/.zprofile`
already edited. Test 9 asserts the destination file was not mutated
(`tests/bootstrap.test.sh:287-288`) but says nothing about the profile, so the behaviour is
unspecified rather than chosen. Either move `ensure_path_export` after `ensure_symlink` - which also
matches the card's own staging intent - or assert the current behaviour deliberately.

### 5. MINOR - the temp symlink leaks when `mv` fails

`bootstrap.sh:119-121` has no trap or cleanup, so any `mv` failure leaves
`~/.local/bin/.jarvis.bootstrap.<pid>` behind under `set -e`. A `trap`-based cleanup, or `rm -f` on
the failure path, keeps a failed run from accumulating files in a directory that is about to be put
on the user's PATH.

### 6. MINOR - the symlink-resolution loop has no iteration cap

`bootstrap.sh:9-16` and `bin/jarvis:4-11` loop `while [ -h "$SOURCE" ]` with no bound, so a symlink
cycle spins forever rather than failing. `bin/jarvis` is the shared entry point every command goes
through, which makes the cheap guard (a counter capped at roughly 40, then `die`) worth having.

## What is good

- The root-resolution fix in `bin/jarvis:3-12` is the correct idiom, is genuinely required by the
  symlink this task installs, is recorded on the card with the user's approval, and is actually
  exercised end to end: `verify` invokes `"$JARVIS_LINK" status` (`bootstrap.sh:143`), so the fresh
  and path-with-spaces cases run `bin/jarvis` *through* the installed symlink. Removing the loop
  would turn those cases red.
- The fixture isolation is real: per-case HOME, a curated PATH directory, its own copy of the clone
  plus the three libs `bin/jarvis` sources, and `never_call` shims that make an unexpected `brew` or
  `curl` invocation a test failure. Nothing reaches the real machine.
- `TMP="$(cd "$(mktemp -d)" && pwd -P)"` with the comment explaining the
  `/var` vs `/private/var` mismatch is exactly the kind of trap that normally costs a debugging
  session.
- The interactive `pty` driver covers the one branch a pipe cannot fake, in both directions.
- Idempotence is properly asserted rather than claimed: case 2 fails if `brew` or `curl` is invoked
  at all, and case 7 runs bootstrap twice and counts the profile line.
- Homebrew is printed and never executed, and case 3 asserts no `.zprofile` and no symlink were
  created on that path.
- `docs/herdr-runtime.md` explains *why* the resolution loop exists rather than only that it does.

## Required to close round 1

Findings 1 and 2, each with the covering test case that currently cannot fail. Finding 3 is cheap and
in the same function as 1, so it belongs in the same round. Findings 4-6 are the engineer's call; say
so explicitly on the card if any is deliberately left alone.

---

# 260902-1204-001 - shell-reviewer, round 2 of 2

**Verdict:** REQUEST_CHANGES (one blocker, a two-line fix in the test harness; the production
script is sound)
**Delta reviewed:** `c29e209..cf2cbe6`, fix tip `015dd91` (code), `cf2cbe6` (card/report)
**Scope:** the delta only, per the round-2 rule. `bootstrap.sh`, `bin/jarvis`,
`tests/bootstrap*.test.sh`, `README.md`, `docs/herdr-runtime.md`. Report and plan files out of
review scope. Checks re-run in full, as required.

## Checks re-run by the reviewer (not taken from the engineer summary)

- `bash tests/bootstrap.test.sh` - 13/13 cases ok.
- `bash tests/bootstrap-interactive.test.sh` - 3/3 cases ok.
- Full suite, every `tests/*.test.sh` individually - 21/21 PASS, 0 failures.
- `shellcheck bootstrap.sh` - clean, no output, exit 0.
- `shellcheck bin/jarvis` - exit 1 on pre-existing findings only: SC1091 x3 on the
  `. "$ROOT/scripts/..."` lines and SC2034 on `HARNESS_HERDR_SESSION` (line 90). The diffed hunk
  (lines 3-17) is clean. Same set as round 1.
- `shellcheck tests/bootstrap*.test.sh` - five SC2016 info notes on the deliberately
  single-quoted `export PATH="$HOME/..."` literals. Correct as written.
- `scripts/plan-check.sh` - ok. `scripts/owns-check.sh` - ok, 1 active card, 4 claims, no overlap.
- `scripts/review-rounds.sh 260902-1204-001` - exit 0, `recorded 1 / ran 0 / one round left`. The
  transcript count is a verified zero because both rounds ran in a session under the reviewer role
  rather than a Herdr reviewer tab; the card headings are the round of record. The card still needs
  a `## Review round 2` heading for this round.
- Scope: every changed path is inside the card's `Owns:` set plus plan/report files.
  `tasks/plan.md` and `tasks/todo.md` untouched, as briefed. No non-English text in the delta.

## Round-1 findings: all six verified closed

Verified by running the ROUND-2 tests against the PRE-FIX script (`c29e209:bootstrap.sh` and
`c29e209:bin/jarvis` in a scratch tree with the current tests and libs, `fail()` made non-fatal so
one run reports every case). This is the check that distinguishes a regression test from a test.

| # | Fix | Pre-fix result of the new coverage |
|---|-----|-----------------------------------|
| 1 | `ensure_local_bin_on_path`, called in `main` before the installers | case 10 FAILS pre-fix: `error: herdr installation did not put herdr on PATH`, exit 1, plus claude and the final banner |
| 2 | `-d` rejected outright at `bootstrap.sh:135`; explicit `rm -f` before `mv` at `:145` | case 12 FAILS pre-fix on all three assertions, littering `.jarvis.bootstrap.98804` in the old target dir - the exact round-1 measurement. Interactive case 3 FAILS pre-fix on the litter assertion |
| 3 | `ZPROFILE="${ZDOTDIR:-$HOME}/.zprofile"` at `:25`, used at `:101-106`, `:166` | case 13 FAILS pre-fix on both assertions |
| 4 | `ensure_symlink` before `ensure_path_export` (`main:183-184`) | case 9's new assertion FAILS pre-fix |
| 5 | `trap 'rm -f "$tmp_link"' EXIT` at `:141`, cleared at `:147` | verified separately (below) |
| 6 | 40-hop cap in both scripts | no coverage; see new finding 2 |

Two details worth recording because they cut against the engineer's own summary in the honest
direction, and the card already says so:

- **Case 11 (non-interactive directory) PASSES pre-fix.** It is a guard, not a regression case,
  because `confirm_replace` already refused non-interactively. The card states this explicitly
  rather than claiming the case as a regression. Correct as labelled.
- **Finding 5's trap was measured, not assumed.** `tmp_link` is a function-local and the trap body
  is single-quoted, so the concern is whether the local is still in scope when the trap fires
  during `set -e` unwinding. Reproduced with a standalone fixture forcing `mv` to fail:
  `TRAP fired, tmp_link=[/var/.../.stage.84005]`, temp link removed, exit 1. The local is visible.
  `grep -n trap bootstrap.sh` confirms no other trap exists, so `trap - EXIT` at `:147` clobbers
  nothing.

## New findings

### 1. BLOCKING - the `ZDOTDIR` fix leaks the host's `ZDOTDIR` into the fixture, so the suite writes to the developer's real zsh profile

`bootstrap.sh` now resolves `ZPROFILE` from `${ZDOTDIR:-$HOME}` (`bootstrap.sh:25`), but neither
test runner pins `ZDOTDIR`: `run_bootstrap` (`tests/bootstrap.test.sh:134-138`) and
`run_bootstrap_pty` (`tests/bootstrap-interactive.test.sh:79-84`) set `HOME`, `PATH` and
`BOOTSTRAP_TEST_BIN` on the invocation and let everything else inherit. On any host with `ZDOTDIR`
exported - anyone keeping zsh dotfiles under `~/.config/zsh`, which is a common layout - the
profile write escapes the fixture.

Measured on the review host (`ZDOTDIR` is unset here, so a sentinel directory was supplied):

- `ZDOTDIR=/tmp/.../zdotdir-sentinel bash tests/bootstrap.test.sh` -> the suite goes RED at case 1
  (`FAIL: fresh install: PATH export missing from ~/.zprofile`), and the sentinel afterwards
  contains a real `.zprofile` holding `export PATH="$HOME/.local/bin:$PATH"`.
- `ZDOTDIR=/tmp/.../zdotdir-sentinel2 bash tests/bootstrap-interactive.test.sh` -> **passes green,
  3/3 ok**, and the sentinel again contains a written `.zprofile`. So the interactive suite mutates
  a file outside the fixture with no signal whatsoever.

Two distinct consequences: the suite's stated contract is broken (`tests/bootstrap.test.sh:1-5`:
"Nothing here ever touches the real machine"), and the non-interactive suite produces a false RED
that depends on an inherited variable it does not control - the kind of failure that reads as a
real defect to whoever hits it. The write itself is one idempotent line, not destructive, which is
why this is a two-line fix rather than a redesign.

The repo already has the right pattern for exactly this, one file over and for the sibling feature:
`tests/jarvis-cli.test.sh:100-101` pins `ZDOTDIR="$TMP/zsh"` on each `bin/jarvis install-alias`
invocation. The delta copied the production convention from `bin/jarvis:108`
(`${ZDOTDIR:-$HOME}/.zshrc`) without copying the test convention that goes with it.

*Fix:* add `ZDOTDIR="$dir/home"` to the env prefix in both `run_bootstrap` and `run_bootstrap_pty`,
and have case 13 pass its custom directory per-invocation (`ZDOTDIR="$D/home/customzdotdir"
run_bootstrap "$D"`, via an optional override) instead of `export`ing into the whole suite
(`tests/bootstrap.test.sh:390-392`). With the runners pinned, case 13 keeps its meaning and the
other twelve cases stop depending on the host.

### 2. MINOR - the 40-hop cap is unreachable for both entry points, and the doc oversells it

`docs/herdr-runtime.md` now states the cap "turns a symlink cycle into a clear failure instead of a
hang". Measured: a symlink cycle never hung, before or after the fix. The kernel resolves the
script path at `exec`, so bash never reads the file:

- post-fix, `bash <cycle>` -> `bash: /tmp/.../link-a: Too many levels of symbolic links`
- pre-fix, same cycle, same tree -> byte-identical message

`getconf SYMLOOP_MAX` on this host is **32**, below the 40-hop cap, so any chain bash can actually
`exec` is acyclic and shorter than the cap by construction. The guard is harmless and cheap, and I
asked for it in round 1, so it should stay - but the committed sentence describes a failure mode
that measurement does not support, which is the one thing a permanent doc line must not do.

*Fix:* keep the cap; reword the doc to what was measured, e.g. that the cap bounds the script's own
resolution loop, while a cycle in the invocation path is already refused by the OS at `exec`.
Optionally lower the cap to sit under `SYMLOOP_MAX`. No code change required.

## What is good

- Finding 1's fix is placed correctly: `ensure_local_bin_on_path` mutates only the running
  process's PATH (`bootstrap.sh:76-78`) and is deliberately separate from `ensure_path_export`,
  with a comment saying why. The `LOCAL_BIN_WAS_ON_PATH` flag captured in `main:175` before the
  mutation is the right way to keep `verify`'s PATH message honest once PATH has been changed
  underneath it - a naive fix would have made `verify` always print "already on PATH".
- Finding 2's fix rejects the directory case *before* `confirm_replace`, so the script never asks a
  question whose "yes" it cannot honour. The `rm -f` carries a comment explaining precisely why a
  bare `mv` is wrong, which is the non-obvious BSD behaviour a future reader would otherwise
  re-derive. Branch order (`-L`, then `-d`, then `-e`) correctly routes a symlink-to-directory to
  the silent-repair path and a real directory to `die`.
- The new fixture `fake_curl_to_home` (`tests/bootstrap.test.sh:104-125`) models what the real
  installers do rather than what the old fixture found convenient, which is what made finding 1
  invisible in round 1.
- `docs/herdr-runtime.md` and `README.md` were both updated to match the new behaviour, including
  the publish-order rationale for finding 4. Only the cycle sentence overreaches.
- Every changed path is inside the declared `Owns:` set; the card's fix-delta account is accurate
  claim by claim, including the caveat that case 11 is not a true regression case.

## Required to close

1. Pin `ZDOTDIR` in `run_bootstrap` and `run_bootstrap_pty`; keep case 13 meaningful by passing its
   directory per-invocation. (New finding 1, blocking.)
2. Reword the `docs/herdr-runtime.md` cycle sentence to the measured behaviour. (New finding 2.)

Both are contained to two files and neither touches the production logic that rounds 1 and 2 have
now read. The round ceiling is spent, so per `RULES.md` this goes back as a targeted fix plus a
check of those two hunks, not a third full review.
